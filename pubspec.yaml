name: mobile_to_adaptive
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  googleapis: ^10.1.0
  http: ^0.13.5
  provider: ^6.0.5
  url_launcher: ^6.1.9
  flex_color_scheme: ^6.1.2
  go_router: ^6.0.9
  split_view: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - ./assets/img/